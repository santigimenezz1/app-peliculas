import React from "react"
const NumeroTemporada = ( {temporada} )=>{
    return (
        <div>
            <h1 style={{color:"white", fontSize:"15px",cursor:"pointer", display:"inline-block"}}>{temporada}</h1>
        </div>
    )
}
export default NumeroTemporada